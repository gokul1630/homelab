# Add this job to your prometheus.yml scrape_configs section:

scrape_configs:
  - job_name: "homelab_exporter"
    static_configs:
      - targets: ["homelab-exporter:9877"]
    scrape_interval: 60s
    scrape_timeout: 30s
