- name: Homelab
  hosts: gokul
  roles:
      - common
      - role: docker
        become: true
        vars:
            ansible_user: "gokul"
      - role: jellyfin
        become: true
      - role: immich
        become: true
        vars:
            ansible_user: gokul
            immich_path: /tank/immich
